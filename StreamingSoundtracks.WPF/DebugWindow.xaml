<Window x:Class="StreamingSoundtracks.DebugWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StreamingSoundtracks"
        mc:Ignorable="d"
        Title="Debug" Width="500" Height="200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ProgressBar Grid.Row="0" Maximum="{Binding BufferLength, Mode=OneWay}" Value="{Binding WritePosition, Mode=OneWay}"/>
        <StackPanel Grid.Row="0">
            <TextBlock>Write</TextBlock>
            <TextBlock Text="{Binding WritePosition, Mode=OneWay}"/>
        </StackPanel>
        <ProgressBar Grid.Row="1" Maximum="{Binding BufferLength, Mode=OneWay}" Value="{Binding ReadPosition, Mode=OneWay}"/>
        <StackPanel Grid.Row="1">
            <TextBlock>Read</TextBlock>
            <TextBlock Text="{Binding ReadPosition, Mode=OneWay}"/>
        </StackPanel>
    </Grid>
</Window>
